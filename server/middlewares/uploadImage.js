const express = require("express");
const upload = require("../utils/multer");
const { Listing, Image } = require("../models/listingModal");
const router = express.Router();

module.exports = router;
